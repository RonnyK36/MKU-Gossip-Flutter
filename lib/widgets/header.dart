import 'package:flutter/material.dart';

AppBar header(context) {
  return AppBar(
    title: Text(
      'MKU Gossips',
      style: TextStyle(
        fontFamily: 'Signatra',
        fontSize: 40,
      ),
    ),
    centerTitle: true,
  );
}
